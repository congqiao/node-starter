image: node:alpine

cache:
  key: $CI_COMMIT_REF_SLUG
  paths:
    - node_modules/

before_script:
  - npm i

stages:
  - build
  - lint

build typescript:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/

lint eslint:
  stage: lint
  script:
    - npm run lint:eslint

lint prettier:
  stage: lint
  script:
    - npm run lint:prettier
